create database mule;
use mule;
/*Here we use the database*/
create table emp(EMPNO INT(11), ENAME varchar(10), JOB varchar(10), MGR int(10), HIREDATE varchar(10), SALARY int(10), CONM int(5), DEPTNO int(5));
